package domain;

/**
 * Clase que representa una podadora.
 */
public class Lawnmower extends Entity {

    public Lawnmower(int line, int xPos) {
        super(line, xPos);
    }

    /**
     * Método que activa la podadora y elimina a los zombies en la línea.
     */
    public void activate() {
        // Lógica para mover la podadora y eliminar zombies
    }
}
